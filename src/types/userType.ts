export type userType = {
  email: string;
  fullname: string;
  role: string;
  address?: string;
  firstName?: string;
  lastName?: string;
  image?: string;
  id: string;
  password: string;
  phone?: string;
};